{
  "name": "sora-ecommerce",
  "version": "1.0.0",
  "description": "Sora E-Commerce Store",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
